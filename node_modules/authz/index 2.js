var expr = require('expr')

function authz(principal) {
  if (!(this instanceof authz)) {
    return new authz(principal)
  }

}

authz.prototype.has = function (permissionExpr) {
  var e = expr(permissionExpr)
}

authz.prototype.is = function (roleExpr) {
  var e = expr(roleExpr)
}

authz.prototype.to = function (subject) {

}

module.exports = authz